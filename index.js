const testVar = {}

function testFunc() {
  return "hi"
}

// function superBowlWin(record) {
//   record.find(result => result === "W")
//   return record.year;
// }

const superBowlWin = record.find(r => r.result === "W" )